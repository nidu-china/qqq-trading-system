# QQQ 0DTE 交易策略

本项目支持三种策略模式，通过 `.env` 中 `STRATEGY_MODE` 切换：
`boll_macd`（分时 BOLL/MACD 做 T）、`trend`（Opening Range Breakout 趋势跟踪）、
`hybrid`（自动切换）。所有策略只用已收盘的 1 分钟常规交易时段 K 线，
时间均为美东时间（ET）。

---

## A. Trend ORB 趋势跟踪策略（`STRATEGY_MODE=trend`）

在趋势日捕捉开盘区间突破，顺势持有至 EMA 反转或 13:55 强制平仓；
在震荡日识别 VWAP 反复穿越后主动放弃交易。

### A.1 Opening Range 构建

| 参数 | 值 |
|------|-----|
| OR 窗口 | 09:30–09:40（包含开盘首 10 分钟） |
| OR High | 窗口内所有 1m K 线最高价 |
| OR Low | 窗口内所有 1m K 线最低价 |

### A.2 时间状态机

| ET 时间 | 状态 | 行为 |
|---|---|---|
| 09:30–09:40 | `OR_BUILD` | 构建开盘区间，不开仓 |
| 09:40–11:30 | `ENTRY` | 检测突破 + EMA/VWAP 对齐，可入场 |
| 11:30–13:55 | `MANAGE_ONLY` | 不再生成新信号，管理已有仓位 |
| 13:55 后 | `FLAT` | 所有仓位无条件清仓 |

### A.3 Call 入场条件（全部满足）

1. 连续 3 根 1m K 线 close 高于 OR High；
2. EMA(9) > EMA(21)，或 EMA(21) 不成熟时 close > EMA(9)；
3. close > 当日累计 VWAP；
4. 当日尚未确认过任何突破方向（每日限 1 个方向）；
5. 非震荡日（VWAP 交叉不超过 3 次）；
6. 当前时间 < 11:30。

Put 使用完全对称条件（close < OR Low、EMA(9) < EMA(21)、close < VWAP）。

### A.4 震荡日检测

从开盘起累计统计每根 bar 的 close 相对当日 VWAP 的位置变化，每次从一侧
穿越到另一侧计为 1 次。累计交叉超过 3 次时标记为震荡日，不再生成任何信号。

### A.5 退出规则

- EMA(21) 穿越：Call 持仓连续 2 根 close 低于 EMA(21) 时全部退出，Put 对称。
- OR 中点穿越：Call 持仓 close 跌破 OR 中点（(OR High + OR Low) / 2）时退出。
- 风控止损止盈（与 BOLL/MACD 策略共享，见下文）。

---

## B. 分时 BOLL/MACD 做 T 策略（`STRATEGY_MODE=boll_macd`）

### B.1 时间状态机

| ET 时间 | 状态 | 行为 |
|---|---|---|
| 09:30–09:35 | `PREHEAT` | 指标预热，绝对不开仓 |
| 09:35–09:42 | `OPENING_SCALP` | 允许独立的 BOLL/价格/成交量爆量趋势信号 |
| 09:42–09:45 | `EXIT_ONLY` | 不开新仓；09:45 清空开盘仓位 |
| 09:45–12:00 | `BOLL_MACD` | 允许产生新仓信号 |
| 12:00–13:55 | `MANAGE_ONLY` | 不再开新仓，只管理和卖出已有仓位 |
| 13:55 后 | `FLAT` | 战术仓归零，当天停止交易 |

### B.2 唯一入场指标

- BOLL：`20, 2`。
- MACD：`8, 17, 9`。
- RSI：`14`，仅作为过热过滤器。
- 成交量均线：前 20 根 1 分钟 K 线的均量。开盘不足 20 根时，使用历史交易日
  相同分钟的成交量均值。

#### B.2.1 开盘爆量策略

09:35–09:42 的开盘信号独立判断，不使用普通 Call/Put 的 MACD 和 RSI 过滤，
也不使用中轨震荡次数门控：

- Call：成交量严格大于 `1.2 × 均量`，收盘价同时高于 BOLL 中轨、当前开盘价
  和前一根收盘价。
- Put：成交量严格大于 `1.2 × 均量`，收盘价同时低于 BOLL 中轨、当前开盘价
  和前一根收盘价。

#### B.2.2 主时段 BOLL/MACD 策略

09:45–12:00 的 Call 和 Put 入场首先经过趋势门控：统计最近 20 根已完成的
1 分钟 K 线相对各自 BOLL 中轨的位置变化；穿越中轨不超过 2 次才允许交易。
当前 K 线刚形成的有效突破不计入历史震荡次数。超过 2 次视为中轨附近反复震荡，
当时不生成任何新仓信号。
盈利退出后，如果同方向连续 5 根收盘都稳定在 BOLL 中轨同一侧，则把历史中轨穿越次数重置为 0，允许重新判断趋势延续入场；重新建仓成交后，本次重置资格即消耗。
趋势延续再入场仍须满足普通入场的全部硬条件，并额外满足：当前量比高于前一根量比；价格沿交易方向距离中轨不超过 `1.2 × BOLL 半带宽`；如果 MACD 柱刚穿越零轴，则量比必须严格大于该方向动态量比门槛的 `1.2` 倍。该过滤只作用于盈利退出后的再入场，不改变首次入场条件。

Call 必须同时满足：

1. 收盘价在 BOLL 中轨上方；
2. 最近三根 BOLL 中轨满足“当前 > 前一根 > 前两根”，确认中轨连续上倾；
3. MACD 柱状图大于 0；
4. 当前 MACD 柱状图大于前一根柱状图；
5. 当前成交量严格大于 `1.2 × 均量`；
6. RSI 严格小于 70。

Put 使用对称条件：

1. 收盘价在 BOLL 中轨下方；
2. 最近三根 BOLL 中轨满足“当前 < 前一根 < 前两根”，确认中轨连续下倾；
3. MACD 柱状图小于 0；
4. 当前 MACD 柱状图小于前一根柱状图，确认空头动能增强；
5. 当前成交量严格大于 `1.2 × 均量`；
6. RSI 严格大于 30，避免在超卖区追 Put。

条件在 1 分钟 K 线收盘后成立即买入，不等待回踩或反转 K 线。
信号只建立一次完整计划仓位，持仓期间不再生成加仓订单。

#### B.2.3 VIX 1分钟趋势动态阈值

使用决策时点之前最近5根已完成、未过期的VIX 1分钟K线判断趋势。最近5根K线中至少
2/3为阴线且收盘相对5根前下跌至少0.5%时判定为VIX下跌趋势；最近5根K线中至少2/3为
阳线且上涨至少0.5%时判定为VIX上涨趋势，否则保持中性。

- VIX下跌：Call量比门槛降低10%至1.08，Put量比门槛提高10%至1.32。
- VIX上涨：Put量比门槛降低10%至1.08，Call量比门槛提高10%至1.32。
- VIX中性或数据不足：继续使用基础阈值，即量比1.2、Call RSI<70、Put RSI>30、
  中轨穿越不超过2次。

BOLL中轨、MACD、RSI和穿越次数始终是固定硬条件，不随VIX放宽。VIX动态阈值参与开盘策略的
方向性量比判断和主时段策略；后续VIX风险状态过滤仍可拒绝已生成的信号。

### B.3 止损和离场

- MACD 快速反转退出使用每根已收盘 1 分钟 K 线判断，不等待 BOLL 确认。Call
  持仓要求 MACD 柱从正值穿越至 0 或负值且当前为阴线；Put 持仓要求 MACD 柱
  从负值穿越至 0 或正值且当前为阳线。成交量还须满足以下任一条件：RVOL
  大于等于 1.20；或 RVOL 大于等于 1.00 且高于前一根 RVOL。三项同时成立
  立即全部退出。
- MACD 穿越时若 K 线方向或成交量没有确认，只记录待确认反转，不退出。后续
  MACD 保持在反向一侧且成交量满足上述条件时全部退出；MACD 返回持仓原方向
  时取消待确认状态。MACD 柱仅在零轴同一侧缩短不建立待确认状态。
- BOLL 中轨反转是独立的第二种退出条件，不再与 MACD 条件绑定：Call 持仓
  连续 2 根收盘都低于各自 BOLL 中轨时全部退出；Put 持仓连续 2 根收盘都高于
  各自 BOLL 中轨时全部退出。趋势底仓继续使用单根收盘穿越 BOLL 中轨退出。
- 期权 Bid 比实际入场价下跌 25%：等待当前 1 分钟 K 线结束；收盘确认时 Bid 仍低于止损线才全部止损。
  分钟内短暂跌破后修复不触发止损。确认后优先提交市价卖单，仅在市价单异常或部分成交时对剩余
  仓位使用最多 3 轮激进限价追单。成交价仍可能因跳空或买盘断层低于止损线，必须记录确认 Bid、
  实际成交价和止损穿透。
- 持仓满 20 分钟且期权 Bid 低于成本：全部退出。
- 开盘仓位在 09:45 无条件清仓。
- 13:55 所有仓位无条件清仓。
- 移动止盈只在期权最高浮盈至少达到 25% 后启动；最高浮盈回吐 30% 时卖出约一半。若只有 1 张则不卖出，直接转为趋势底仓。剩余趋势底仓不再使用移动止盈或 20 分钟亏损退出，Call 在收盘跌破 BOLL 中轨或 MACD 柱转为非正且继续减弱时退出，Put 完全对称；否则可持有至 13:55。

本策略不使用 EMA/VWAP/ATR 结构止损。
项目现有的分批止盈、盈利回撤保护和超时退出仍然生效。

---

## C. Hybrid 混合策略（`STRATEGY_MODE=hybrid`）

同时运行 Trend ORB 和 BOLL/MACD 两个引擎：

1. **突破确认前**：两个引擎同时评估；BOLL/MACD 信号直接通过（保留开盘窗口交易）。
2. **ORB 突破确认**：锁定为 `trend` 模式，当日只用 Trend ORB。
3. **10:00 前无突破或检测到震荡**：锁定为 `boll_macd` 模式。
4. 退出逻辑跟随开仓时所用的策略。

---

## D. 合约、流动性和 VIX

0DTE 合约选择、报价质量、手续费、滑点、每日最多交易次数及 VIX 方向过滤继续使用
项目统一风控。策略不设置每日亏损熔断。

## E. 回测

- 优先使用历史期权 Ask 入场、Bid 退出。
- 历史期权报价缺失时，使用 0DTE Black-Scholes 按每根 1 分钟 K 线重新定价，不沿用上一根
  合成价格，避免路径依赖。
- 合成 IV 只使用决策时点之前的数据：近期真实期权报价反推值优先，其次为 VIX、历史波动率、
  默认 IV；不得读取未来报价。
- 合成报价按美分档位生成动态 Bid/Ask，并在结果中标记定价模型、IV 来源和报价完整性。
- 真实持仓的某分钟报价缺失时，仅对缺口分钟进行模型补价，仍执行止损和趋势退出；真实报价恢复后
  立即改回实际 Bid，结果记录模型补价分钟数。

```powershell
qqq-trader backtest --bars <QQQ数据路径>
```

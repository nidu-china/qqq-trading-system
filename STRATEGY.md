# QQQ 0DTE 动态价格结构交易策略

本文档是 Paper、Live 和 Replay 三种模式的唯一交易规则来源。三种模式必须共用同一套
1 分钟状态机、VIX 过滤器、合约选择、仓位计算和离场逻辑。

## 1. 基本原则

- 标的为 QQQ，当天只交易当日到期的 Call 或 Put。
- 所有技术判断只读取当时已经收盘的常规交易时段 1 分钟 K 线。
- 不使用固定开盘区间高低点，也不等待 5 分钟 K 线确认。
- 最近 10 根已收盘 K 线的平台高低点用于判断动态突破。
- 最近 5 根已收盘 K 线的高低点用于判断回踩、反弹和结构止损。
- 信号 K 线收盘后立即进入期权执行流程，不等待下一根 K 线收盘。
- 同一根 K 线最多生成一个入场信号，优先级为反转、趋势回踩、趋势突破、震荡。
- 没有满足任一完整状态时进入 `UNKNOWN`，不交易。

## 2. 交易时间

| ET 时间 | 行为 |
|---|---|
| 09:30–09:44 | 观察和指标预热，不开仓 |
| 09:45–11:29 | 允许产生新仓信号 |
| 11:30–13:54 | 只管理已有仓位 |
| 13:55 | 无条件清仓 |

系统从 09:30 启动，但最早入场时间固定为 09:45。每日最多开仓 5 次，完全平仓后冷却 5 分钟。

## 3. 指标和关键数据

默认指标：

- BOLL(20, 2)。
- RSI(14)，超买 70，超卖 30。
- EMA9、EMA21。
- MACD(5, 10, 3)。
- ADX(14)。
- ATR(14)。
- 当日 VWAP 及过去 5 分钟的 VWAP 变化。
- 前日最高价、前日最低价、前日收盘价。
- 当日最高价、当日最低价。
- 最近 10 根 K 线的动态阻力和动态支撑。
- 最近 5 根 K 线的回踩低点和反弹高点。

EMA、MACD、RSI、ATR 和 ADX 使用最多最近 500 根常规交易时段 K 线预热，可以包含前一交易日。VWAP、当日高低点每天重置。

## 4. 动态价格结构

当前 K 线不参与自身突破边界的计算：

```text
动态阻力 = 当前 K 线之前最近 10 根完整 K 线的最高价
动态支撑 = 当前 K 线之前最近 10 根完整 K 线的最低价
```

回踩结构：

```text
回踩低点 = 当前 K 线之前最近 5 根完整 K 线的最低价
反弹高点 = 当前 K 线之前最近 5 根完整 K 线的最高价
```

动态结构随每根已收盘 K 线更新。所有结构止损都增加 `0.1 × ATR` 缓冲，入场价到结构止损的距离超过 `2 × ATR` 时拒绝入场。

## 5. 状态优先级

每根 K 线按以下顺序判断：

1. `REVERSAL_UP` / `REVERSAL_DOWN`。
2. `TREND_RETEST_UP` / `TREND_RETEST_DOWN`。
3. `TREND_UP` / `TREND_DOWN`。
4. `RANGE`。
5. `UNKNOWN`。

先成立的状态优先，后续状态不再检查。

## 6. 状态 A：动态趋势突破

### 6.1 Call

以下条件全部满足：

1. 收盘高于动态阻力 `+ 0.1 × ATR`。
2. 收盘在 VWAP 上方，且过去 5 分钟 VWAP 向上。
3. EMA9 高于 EMA20。
4. 最近三根 K 线中至少两次形成更高高点或更高低点。
5. `60 ≤ RSI < 70`。
6. MACD 柱为正，且 `|MACD柱| ≥ 0.1 × ATR`。
7. 收盘距离 VWAP 不超过 `3 × ATR`。
8. 前日高点没有形成距离小于 `0.5 × ATR` 的直接阻力。
9. 以下四项至少满足两项：
   - MACD 柱高于上一根；
   - ADX 不低于 22；
   - 当前成交量不低于最近 20 根平均成交量的 1.2 倍；
   - 当前收盘高于上一根 K 线最高价。

止损取动态阻力下方和信号 K 线低点下方两个位置中距离入场更近者。

### 6.2 Put

使用完全镜像的条件：

- 收盘低于动态支撑 `- 0.1 × ATR`。
- 收盘在 VWAP 下方且 VWAP 向下。
- EMA9 低于 EMA20。
- 最近三根形成下降结构。
- `30 < RSI ≤ 40`。
- MACD 柱为负且强度足够。
- 前日低点没有形成近距离支撑。

止损取动态支撑上方和信号 K 线高点上方两个位置中距离入场更近者。

## 7. 状态 B：趋势回踩延续

这一状态用于在 EMA、RSI 等滞后指标完全确认前捕捉回踩后的继续运行。

### 7.1 Call

必须同时满足：

1. 当日高低跨度不低于 `2 × ATR`，当前收盘位于当日价格区间中点上方。
2. 最近 5 根 K 线形成了不低于 `0.2 × ATR` 的向下回踩。
3. 当前最低价高于最近 5 根回踩低点，形成更高低点。
4. 当前为阳线，收盘位于自身振幅上半部。
5. 收盘重新接近或突破上一根 K 线高点，允许 `0.1 × ATR` 的误差。
6. MACD 柱由负转正，并且高于上一根。
7. `40 ≤ RSI < 70`。
8. EMA9 可以暂时低于 EMA20，但距离不得超过 `0.5 × ATR`。
9. 收盘可以暂时位于 VWAP 下方，但不得低于 `VWAP - 0.5 × ATR`。

09:45–10:00 允许最低点出现后立即确认。10:00 后，最近 5 根中的回踩最低点之后必须至少
已经收盘两根 K 线，避免在第一次反弹时过早买入。

Call 止损：

```text
最近 5 根回踩最低点 - 0.1 × ATR
```

### 7.2 Put

使用完全镜像的条件：

- 当前收盘位于当日区间中点下方。
- 最近 5 根形成向上反弹。
- 当前最高价低于最近 5 根反弹高点，形成更低高点。
- 阴线收于自身振幅下半部。
- MACD 柱由正转负。
- `30 < RSI ≤ 60`。
- 价格与 EMA20、VWAP 的偏离仍在允许范围内。

Put 止损放在最近 5 根反弹高点上方 `0.1 × ATR`。

## 8. 状态 C：动态突破失败后的反转

反转观察使用启动前最近 10 根 K 线形成的固定动态支撑或阻力。观察启动后，收回水平不再
随着后续 K 线移动。

### 8.1 Call

1. 连续两根收盘跌破动态支撑，或者最低价低于动态支撑 `- 0.2 × ATR`。
2. 观察期间 RSI 曾经不高于 30。
3. 15 分钟内重新收于启动时的动态支撑上方。
4. RSI 从 30 下方重新升到 30 上方。
5. MACD 柱高于上一根。
6. 当前 K 线没有再创新低，且收盘位于自身振幅上半部。
7. 距离 VWAP 不超过 `3 × ATR`。

站上 VWAP 使用正常仓位；仍在 VWAP 下方使用正常仓位的 50%。

止损放在观察期间最低价下方 `0.1 × ATR`。

### 8.2 Put

条件完全镜像：突破动态阻力失败、RSI 从 70 上方跌回、MACD 转弱、没有再创新高，并在
15 分钟内收回启动时的动态阻力下方。

止损放在观察期间最高价上方 `0.1 × ATR`。

## 9. 状态 D：震荡下轨做多

趋势和反转均不成立，并且以下五项至少满足四项：

1. 最近 20 根 K 线总振幅不超过 `4 × ATR`。
2. 最近 20 分钟价格穿越 VWAP 至少三次。
3. ADX 不高于 18。
4. `|EMA9 - EMA20| ≤ 0.25 × ATR`。
5. 过去 5 分钟 VWAP 变化不超过 `0.2 × ATR`。

额外要求：

- 最近 20 根价格路径的方向效率不超过 45%，避免把缓慢单边误判为震荡。
- 当前 K 线振幅不超过 `2 × ATR`。
- 当前价格距离前日低点至少 `0.3 × ATR`。

Call 入场：

- 触碰 BOLL 下轨后重新收在下轨上方；
- RSI 不高于 30，或者刚从 30 下方回升；
- MACD 柱高于上一根。

止损放在最近 5 根最低价下方 `0.1 × ATR`。

震荡策略第一版不在 BOLL 上轨买 Put。

## 10. VIX 方向过滤

- `NORMAL`：Call、Put 都允许。
- `RISK_OFF`：只允许 Put。
- `RECOVERY`：只允许 Call。
- `SHOCK`：禁止开仓。
- `UNAVAILABLE`：禁止开仓。

默认使用 VIX 20 日分位、5 分钟和 15 分钟变化。VIX 数据过期或历史不足时归入
`UNAVAILABLE`。

## 11. 期权选择与执行

- 到期日必须是当前交易日。
- 对目标方向最接近 QQQ 现价的 5 个行权价并行获取报价。
- 过滤无效 Bid/Ask、交叉报价、过期报价和流动性不足的合约。
- 相对价差不超过 10%，绝对价差不超过 0.20 美元。
- 未平仓量不低于 100，成交量不低于 10。
- 优先选择绝对 Delta 最接近 0.45 的合约；无 Delta 时选择最近 ATM。

入场：

```text
最高允许买入价 = 信号时 Ask + 0.02 美元
```

- 信号有效期 60 秒。
- 初始使用 Ask 限价，最多重新报价两次。
- 每次订单最多等待 6 秒。
- 不得跨 K 线复用未成交信号。

## 12. 仓位和统一风控

- Paper 初始权益为 10,000 美元；Live 从 Longbridge 实时读取权益。
- 单次权利金预算不超过权益 5%。
- 最大合约数 10 张。
- 每日最多开仓 5 次。
- 当日亏损达到开盘权益 2% 时立即平仓并停止交易。
- 每张每边手续费 1.50 美元。
- 每边报价滑点缓冲 0.02 美元。
- 计划亏损包含 25% 权利金止损、双边手续费和退出滑点。
- 新仓计划亏损不得超过当日剩余亏损额度。

## 13. 离场

统一离场：

- 期权可执行 Bid 比实际入场价下跌 25%：全部止损。
- QQQ 收盘触发动态结构止损：全部止损。
- 持仓后立即追踪 QQQ 对有利方向的最远价格（Call 记最高价，Put 记最低价）；
  当 QQQ 从最远价格回撤超过 `0.5 × ATR`（使用入场时的 ATR 值）且扣除双边手续费
  后仍盈利时，全部止盈。
- 盈利达到 100%：向上取整卖出一半，剩余止损移到成本。
- 盈利达到 250%：清仓。
- 持仓超过 30 分钟且 Bid 低于入场价：清仓。
- 11:30 对剩余仓位向上取整减半。
- 13:55 无条件清仓。

震荡仓位：

- QQQ 收盘达到 BOLL 中轨：减半并把剩余止损移到成本。
- QQQ 收盘达到 BOLL 上轨：清仓。
- 再次跌破下轨，同时 RSI 和 MACD 恶化：清仓。

11:30 后：

- Call 跌破 VWAP 且 MACD 为负，或有效跌破 EMA20：清仓。
- Put 突破 VWAP 且 MACD 为正，或有效突破 EMA20：清仓。

## 14. 回测合成期权

没有历史期权报价时，使用最近整数 ATM 行权价和以下初始 Greeks：

- Call Delta `+0.45`，Put Delta `-0.45`。
- Gamma `0.055`。
- Vega `0.099`。
- Theta `-3.00/交易日`。

理论价格逐分钟更新，Call Delta 限制在 0.05–0.95，Put Delta 限制在
-0.95–-0.05。缺少 IV 时 Vega 项为零。

合成买价在理论价上增加 0.02，合成卖价减少 0.02；每边另计 1.50 美元手续费。部分止盈
必须保存为同一笔完整交易的 exit legs。

## 15. 决策流程

```text
完整 1 分钟 K 线收盘
→ 更新滚动平台、回踩结构、前日价位和当日高低点
→ 更新 VWAP、EMA、MACD、RSI、BOLL、ATR、ADX
→ 按优先级检查反转、趋势回踩、趋势突破、震荡
→ 计算动态结构止损并检查 2 × ATR 上限
→ 执行 VIX、账户风险、次数、冷却和流动性过滤
→ 选择当日到期期权
→ 在 Ask + 0.02 上限内立即提交限价单
→ 持仓期间持续检查期权和 QQQ 离场条件
→ 13:55 无条件清仓
```

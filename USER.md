# USER — **[你的名字或代号]**

> AI 读这份文件是为了对齐「你是谁、你怎么协作」。请把方括号与斜体占位改成你的真实情况；**「当前状态」建议每周更新一次**。

---

## 工作身份

**职责范围**：个人量化开发与运维——QQQ 0DTE 状态机交易系统（Paper / Live / Replay / Backtest）；策略研发、风控、回测与实盘稳定性。

**对接角色**：主要是自己；券商侧为 Longbridge API；无固定团队评审。

**决策层级**：策略参数、仓位与是否上 Live **由我最终拍板**；AI 负责分析、实现草稿与验证路径，不擅自改生产密钥或扩大实盘风险。

---

## 工作方式

**时间节奏**：*[例如：工作日晚间 + 周末；美东盘前/盘中需要快速响应]*

**信息处理偏好**：先结论与可执行步骤，再附依据；长文用标题与小节；能跑命令/读代码就别空谈。

**沟通习惯**：中文主沟通；错误日志、`.env` 相关现象贴原文（敏感值打码）；偏好在 Cursor 里直接改仓库，而不是只给口述方案。

---

## 判断偏好

在常见二元取舍下的 **默认倾向**：

| 维度 | 默认更倾向 |
|------|------------|
| 速度 vs 稳健 | **稳健**（涉及实盘、资金、下单路径时） |
| 自动化 vs 可解释 | **可解释优先**，再逐步自动化 |
| 新依赖 vs 少依赖 | **少依赖** |
| 完美抽象 vs 能跑能验 | **能跑、能回测/能日志验证** |
| Paper vs Live 先行 | **Paper / Replay 验证通过后再谈 Live** |

**一句话总结**：先验证再上线；小步迭代；实盘相关默认保守。

---

## 背景知识库

**专业术语**（AI 应按本仓库语义理解）：

- `QQQ 0DTE`：标的 QQQ 的当日到期期权；本项目不构成投资建议。
- `STRATEGY_MODE`：`trend`（ORB）、`boll_macd`、`hybrid`（见 `STRATEGY.md`）。
- `Paper` / `Live` / `Replay`：共用同一套策略与风控；首次部署必须 Paper。
- `OR` / `VWAP` / `VIX` 状态：`NORMAL` / `RISK_OFF` / `RECOVERY` / `SHOCK`。
- 时区默认 **美东 ET**；信号基于已收盘 1 分钟 RTH K 线。

**常用工具**：Windows + PowerShell、Cursor、Python、Longbridge、本仓库 `src/qqq_trader/` 与 `scripts/`。

**团队结构**：单人项目。

---

## 当前状态（每周更新）

**更新日期**：*YYYY-MM-DD*

| 项 | 内容 |
|----|------|
| 进行中的项目 | *例如：hybrid 周报、入场定价（Bid 阶梯）、回测细节脚本* |
| 最近在烦的事 | *例如：某路径报价/成交差异、时区或恢复行为边角* |
| 近期重要决定 | *例如：某策略模式优先；Live 暂缓直到 Paper 稳定* |
| 接下来重点 | *例如：对齐 STRATEGY.md 与实现；补回归验证* |

---

## 沟通雷区

**会让我立刻烦的沟通方式**：

- 不问现状就给大重构或换栈方案
- 把假设当事实，又不给出验证方式（回测、日志、最小复现）
- 对实盘/资金相关改动不提示风险，或假装已经执行过危险操作
- 空话客套、重复总结、不贴路径/命令依据

**更喜欢的推进方式**：先对齐约束与成功标准 → 最小改动 → 用 Paper/回测/日志验证 → 再谈是否上 Live。
